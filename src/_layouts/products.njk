---
layout: base
---

<div class="wrapper | flow">
  <h1>{{ title }}</h1>
  <p class="page-desc">{{ description }}</p>
  
  <!-- category list -->
  {% include "partials/product-category-list.njk" %}
  
  {{ content | safe }}
</div>